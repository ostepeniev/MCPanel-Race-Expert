/**
 * Login layout — no sidebar, no navigation.
 * Just the login page centered on screen.
 */
export const metadata = {
  title: 'Вхід · MCPanel',
};

export default function LoginLayout({ children }) {
  return children;
}
